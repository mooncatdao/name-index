{
  "name": "@mooncatdao/name-index",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible index of MoonCat naming events and current names.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/constants.js && npm test"
  },
  "dependencies": {
    "viem": "^2.33.2"
  }
}
